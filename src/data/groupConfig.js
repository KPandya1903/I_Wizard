import { GENERATED_NPC_GROUPS } from './generatedCharacters'

// Use generated groups (dynamically sized based on character count)
export const NPC_GROUPS = GENERATED_NPC_GROUPS

export const GROUP_SCATTER_RADIUS = 3
export const MIGRATION_INTERVAL = 45000
export const ARRIVE_AT_GROUP_DIST = 2.5
export const PLAYER_GROUP_PROXIMITY = 8
